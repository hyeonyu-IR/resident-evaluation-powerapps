# Scripts

This folder is reserved for optional helper scripts.

Examples you may add later:
- a script to refresh deployment ZIP contents
- a script to validate template files
- a script to compare list schemas between releases
